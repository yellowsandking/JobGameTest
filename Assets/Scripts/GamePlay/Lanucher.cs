using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lanucher : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        ManagerAdmin.Instance.Init();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
